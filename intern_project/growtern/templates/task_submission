<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task Submission (Desktop)</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f3e8ff; /* Light purple background */
        }
        .upload-area {
            background-color: #ede9fe; /* Lighter purple */
            border: 2px dashed #a78bfa; /* Medium purple dashed border */
            min-height: 250px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            border-radius: 12px;
            padding: 30px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .upload-area:hover {
            background-color: #dcd6ff; /* Even lighter purple on hover */
        }
        .upload-icon {
            width: 80px;
            height: 80px;
            color: #8b5cf6; /* Vibrant purple */
        }
    </style>
</head>
<body class="flex items-center justify-center min-h-screen p-4">
    <div class="w-full max-w-4xl bg-white rounded-lg shadow-xl overflow-hidden md:flex">
        <!-- Left Section (Navigation/Info) -->
        <div class="w-full md:w-1/3 bg-gradient-to-br from-purple-800 to-purple-600 p-8 text-white flex flex-col justify-between rounded-l-lg">
            <div>
                <button class="mb-8 p-3 rounded-full bg-purple-700 hover:bg-purple-900 transition duration-300">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-6 h-6">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M10.5 19.5L3 12m0 0l7.5-7.5M3 12h18" />
                    </svg>
                </button>
                <h2 class="text-4xl font-extrabold mb-4">Task Submission Portal</h2>
                <p class="text-purple-200 text-lg">Effortlessly submit your tasks and track your progress.</p>
            </div>
            <div class="mt-8">
                <p class="text-purple-300 text-sm">Need help? Contact support.</p>
            </div>
        </div>

        <!-- Right Section (Form Content) -->
        <div class="w-full md:w-2/3 p-8 bg-white flex flex-col justify-center">
            <h1 class="text-4xl font-bold text-gray-800 mb-8 text-center">TASK SUBMISSION</h1>

            <div class="mb-6 bg-purple-900 text-white p-4 rounded-lg shadow-md">
                <p class="text-lg font-semibold">Submit before 15th July</p>
            </div>

            <div class="upload-area mb-8">
                <svg class="upload-icon mb-4" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M12 16.5V9.75m0 0l3.75 3.75M12 9.75L8.25 13.5m-1.5 1.5h-.75A2.25 2.25 0 014.5 14.25v-2.75a2.25 2.25 0 012.25-2.25H9m5.25 6.75h-.75A2.25 2.25 0 0116.5 14.25v-2.75a2.25 2.25 0 012.25-2.25H15m-3 6.75V21m0-12.75V4.5" />
                </svg>
                <p class="text-gray-600 text-xl font-medium mb-2">Drag & Drop your file here</p>
                <p class="text-gray-500 text-base">or click to browse</p>
                <input type="file" class="hidden" id="fileInputDesktop">
            </div>

            <button class="w-full bg-purple-700 text-white py-3 px-6 rounded-lg font-semibold text-lg hover:bg-purple-800 transition duration-300 shadow-lg">
                Submit Task
            </button>
        </div>
    </div>

    <script>
        document.querySelector('.upload-area').addEventListener('click', function() {
            document.getElementById('fileInputDesktop').click();
        });

        document.getElementById('fileInputDesktop').addEventListener('change', function(event) {
            const file = event.target.files[0];
            if (file) {
                console.log('File selected:', file.name);
                // You can add logic here to handle the file upload, e.g., display a preview or send it to a server.
                const uploadArea = document.querySelector('.upload-area');
                uploadArea.innerHTML = `
                    <p class="text-gray-800 text-xl font-medium mb-2">${file.name}</p>
                    <p class="text-green-600 text-base">File selected successfully!</p>
                `;
            }
        });
    </script>
</body>
</html>
